import {ApiServer} from "./ApiServer";
import config from "config";
// const apiServer = new ApiServer(config.has("api.listenPort") ? config.get("api.listenPort") : 8080);
// console.log(`API listening on port ${apiServer.listenPort}`);
